# SpeakMate 3.0 实施决策与代价

更新：2026-09-11。按实施记录中实际作出判断的顺序整理。本附件说明已批准范围内的工程取舍，不新增产品功能，也不表示全部功能已经实现或验收。规格以根目录 `APP_SPEC.md` 为准；具体接口以对应契约、实现及测试证据为准。

|顺序|采用的判断与理由|代价／判断不适合时的影响|
|---|---|---|
|1|先完成各功能入口，再切换五项底部导航，避免中间版本出现空页面。|导航整合较晚，仍可能需要后续布局调整；未删减任何入口。|
|2|保留旧部署，同时在发布前准备能读取 IndexedDB v2 的兼容回退构建。原 2.3 指定打开 v1，不能直接读取已升级数据库。|增加回退构建与数据验证工作；不能以删库或降级弥补。|
|3|备份身份不同且本机仅有自动生成的空游客时，经预览说明和确认可采用备份身份；两个已有学习历史的不同身份不静默合并。|两台设备各自已有历史时，需要以后另做明确的合并流程；本机数据和备份原件保留。|
|4|语料先做咖啡试点和共享契约，再分七个类别批次编写、阅读与审查，最后整合。|增加交接和跨类别整合成本；2,520 问／5,040 答、五级和 42 场景目标不变。|
|5|记录词句后的撤销，只原子撤销本次保存的变更；后续并发编辑冲突时保留数据并提示。|某些撤销需要用户重新核对，并需一个小型事务接口和凭据；不建设通用版本历史系统。|
|6|网址 slug 与持久化场景 ID 分开：例如 coffee-order 对应 dining-01，旧种子顺序不改变。|映射错误会损坏旧会话和备份关联，必须用固定旧样本验证，不能由新映射同时生成测试预期。|
|7|场景显示版本对应场景快照；分级语料版本在对话包与状态中独立固定，不能要求两者相等。|增加关系校验、往返测试及交接修正；不改写历史版本或增加版本服务。|
|8|版本号、manifest、健康接口和缓存版本在最终审查前修改；发布阶段只部署已审查的确切提交。|开发分支会较早带有未发布的 3.0 标识；命名改变需要重新限定审查，不临时补未审查代码。|
|9|离线详情使用固定客户端页面外壳、必要的不透明标识与公开选项；跨页面外壳采用文档导航，同壳使用受支持的 History API。|跨页会重载，需要显式保留来源、滚动与未保存保护，并验证 HTML／RSC／资源缓存隔离。不透明 ID 仍可能出现在同源冷启动请求中。|
|10|多个类别复用相同的问法元数据映射，提取最小共享助手；正文、路径和审校决定仍逐项编写。|增加共享依赖和既有类别回归；助手不生成正文、不自动标记已审阅，不扩大为对话 DSL。|
|11|场景搜索与记录簿搜索均留在本机，不进入新网址、RSC 或嵌套返回链接。|搜索文字不能随网址分享；需本地筛选恢复和存储失败兜底。旧传入网址已包含的搜索请求无法撤回，后续导航须剥离。|
|12|合法的旧版未分级会话保留原文、只读查看；新版由用户明确创建不同 ID 的新练习。|旧版未结束对话不能原地续接，需要清楚说明；不复制旧引擎或批量转换旧完成证据。|
|13|“可接受的备份历史”和“可以继续执行的对话状态”分别校验，不新增多余持久化标记。|需要一致的恢复分类和不可继续提示；保留合法历史不代表能执行它，不补造缺失话轮。|
|14|正常对话到达终局后先等待用户确认；发布前将完成状态、事件、计划、账本及奖励接入同一个受保护事务。|增加确认状态、恢复路径和跨模块事务测试；不能先标完成再事后补积分。普通退出保留进度，明确停止才是放弃。|
|15|版本 1 的有效练习按所选校审路径达到轮次上限且至少有一次非空回答／改答，或确实达成所选流程，再由用户确认。未覆盖表达和混合帮助不受正确率门槛惩罚，纯帮助、无效流程和提前放弃不算完成。|这是偏宽松的投入判定，本地积分仍可能被刷取或篡改；分别展示提交次数与实际表达次数，任务轮数必须对应它固定的真实路径。|
|16|新分级会话固定保存有界、可选的角色与演练说明；从明确映射按场景及必要的等级／变体选取，不解析正文、不改动已审查的语料包。缺字段的旧记录使用中性标签，恢复以保存说明为准，不按最新目录改写历史。|增加展示快照、校验与备份测试；旧标签较通用，错误映射只能修正后续新会话，历史不能静默改写。|
|17|从实际生产输出生成构建绑定的公开离线资源清单，分类资源关系须有证据；有其他作用域内窗口时暂不手动强制激活更新，旧资源清理须考虑迟到访问和新窗口竞态。|增加构建输出校验和框架升级维护；其他窗口即使空闲也可能需要关闭后再更新。清单和窗口检查本身不等于离线或跨窗口原子安全证明。|
|18|实际构建缺少可靠分类依赖图，改用明确的构建期静态分类 JSON 和客户端公开资料适配器；已审查内容不变，个人表达不进入下载请求，本机运行对话与解析。|增加静态资料导出、版本／载荷校验和适配器；首次使用未缓存分类需要下载，但避免首次准备就隐性下载全部语料。|
|19|现有场景图片组件直接使用八张固定同源 WebP，并纳入离线资源清单；仅对该组件关闭动态图片优化，保留布局与替代文字。|这些小图片不再动态按屏宽重编码；需核对直接资源及手机视觉，避免猜测优化器的多种网址。|
|20|生产首次加载分类时，等待现有离线管理器接管并确认实际响应已按构建清单验证；等待最多 10 秒后显示重试／更新指引，不另增清单端点。|首次准备可能较慢；离线管理器不受支持、禁用或安装失败时不能创建依赖未验证分类的新练习／解析。已保存快照、历史与备份仍保留入口，真实浏览器行为另验。|
|21|保留旧备份接受的 ID 范围和新版链接边界；特殊历史编号改为原页只读查看完整记录与导出，不让链接构造失败拖垮页面。|这些非标准编号不能直接链接或通过正常路由续练；可另建练习，原 ID、内容与引用不变。不引入编号别名系统，也不收紧旧备份支持。|
|22|为现有有效解析／来源等级关联编写独立版本化的 3–5 问微练习描述，仅在新建模拟时生成固定快照并复用现有引擎；普通路径和历史不变。|增加有限路径编写、完整组合审校与版本／备份校验；当前待覆盖 277 组关联不能按数量自动宣称通过。练习沿用真实来源等级，未知或无关联内容诚实自练，不把普通短练或任意切片冒充定向练习。|


## 对应的实施依据

- 场景身份与版本：[场景 ID 对照](3.0-scene-identities.md)、[语料分批交付](3.0-corpus-delivery.md)。
- 保存、恢复与完成：[练习持久化契约](3.0-practice-persistence.md)、[学习闭环验收案例](3.0-learning-acceptance-cases.md)。
- 手机导航和词句来源：[离线路由契约](3.0-offline-routing.md)、[记录簿交互](3.0-notebook-flow.md)、[交互契约](3.0-interaction-contracts.md)。
- 当前实现与证据：[3.0 验证与发布记录](../releases/3.0.0-verification.md)。未执行的浏览器、实体设备、回退和发布项目仍是待验收项。

以上为截至本次更新的 22 项判断；后续若出现新决策或推翻既有决定，应先补充这里与对应契约，再实施和验证。这里的取舍说明不能替代最终交付时对实际改动、未完成项目及限制的披露。
