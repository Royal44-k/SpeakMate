# SpeakMate 3.0 候选部署记录（2026-09-13）

## 后续用户反馈与归档边界

用户随后明确反馈“可以访问新版 speakmate”，并要求整理资产上传 GitHub。此反馈记为用户确认的可访问性，不覆盖下文最近一次工具检查，也不推断正式域名已推广或全部在线资源已验收。本次归档不重新部署、不切换域名、不导入学习记录；部署后续状态需再由真实接口证据更新。

## 结论

Vercel 用户授权、现有项目关联、上传清单核对及两个候选版本的云端构建已完成。正式网址没有切换；在线 HTTP／资源完整性验收因当前电脑连接超时尚未完成。此记录不是正式发布通过、实体 iPhone 通过或中国大陆网络通过结论。

## 目标与源码

- 团队：`team_GXUbEGjD0inFlQqufnaKsSVQ`，本轮接口再次确认 Hobby 免费套餐。
- 项目：`prj_EVHJZ8TczAF9yHrDjNz2aiBq0ItM`，`speakmate-pwa`；未创建项目、数据库、付费服务或修改套餐。
- 用户网址：[SpeakMate](https://speakmate-pwa.vercel.app/)。本轮末尾经部署 API 再次确认仍指向旧 `dpl_EUHHJUXwxLJw7FjFjntkrf2ZE1Fh`。
- 候选源码：`9b61835b873a64782242ef0d10eff1ed6bcc62c0`（此前两份文档状态提交）；应用代码与已审查 `c2af5485bf8b73a22242594ae17504deac01c717` 相同。本轮没有修改应用源码。
- 所有候选均使用 `--prod --skip-domain`；部署 API 的 `autoAssignCustomDomains=false`。CLI 的“Aliased”团队域名提示不能当作用户网址已切换的证据。
- 构建及运行环境显式设置本地 AI、同步关闭、对应恢复模式及发布 SHA；没有向云端发送任何学习记录。

## 实际候选部署

| 版本 | 部署 ID | 结果 |
| --- | --- | --- |
| 普通版 | `dpl_8R4ZRiUuh8BGk2GZBUR5dAREW4rx` | READY，构建 33 秒 |
| 应急版首次 | `dpl_2GpyJK5sXSGdqbFM7wi6tCknjr3x` | Next 编译通过，但 Vercel 打包查找错误输出目录而失败 |
| 应急版修正配置后 | `dpl_2DQAHvEu9sLF3hTSdTvbDJCYhNhU` | READY，构建 31 秒 |

普通版：[独立候选地址](https://speakmate-cffj1r5ry-lirongouyang522-3492s-projects.vercel.app/)。应急版：[独立只读候选地址](https://speakmate-aygjxqqwc-lirongouyang522-3492s-projects.vercel.app/recovery)。独立地址保留 Vercel 访问保护，可能需要原部署账号登录。不能在这些独立域名读取正式域名的 IndexedDB；本次不会导入或复制正式学习记录。

两个就绪部署的实际 `projectSettings.nodeVersion` 均为 `22.x`；这是部署配置证据，不是已经读取远端进程版本。构建日志确认 pnpm `11.19.0`、Next.js `16.3.4`，执行 `pnpm run build`，实际命令为 `next build && node scripts/offline-manifest.mjs`。

普通版云端生成结果：

- 构建 ID：`MT7REtpdsjw4wZzxLgBzF`。
- 16 个页面壳，70 个资源，共享资源 5,029,434 字节。
- 七类内容包分别为 754091／859537／761324／870083／670297／687260／808499 字节；顺序为旅行、餐饮、生活、职场、社交、学习、应急。
- 这些是云端构建输出，尚未取得远端实际响应并比对全部 SHA256；不可拿本地 IlMT 构建的清单哈希替代。

应急版保持固定 `NEXT_PUBLIC_RECOVERY_ONLY=true`。失败原因是平台默认寻找 `.next/routes-manifest.json`，而已审查的恢复构建输出为 `.next-recovery`。仅通过部署级配置增加 `outputDirectory: ".next-recovery"` 后成功；没有修改 Next 源码或项目全局设置。该配置路径及原始失败保留于本地任务工作目录。

## 上传、认证与工具边界

用户明确同意后进行了官方设备授权；CLI `whoami` 确认为原账号。授权前受限网络返回 `EACCES`，批准联网执行后成功。没有要求用户发送密码或 Token。

CLI 关联项目自动创建被忽略的 `.env.local`，没有读取、打印或复制到发布包；CLI 追加的重复 `.gitignore` 规则已准确移除。认证配置始终保留在被排除的本地任务目录，不进入 Git 或上传清单。

原工作区 dry-run 得到 304 个实际文件，其中存在本地编译缓存、旧生成清单及开发审校报告。因此创建隔离发布副本，省略 16 个开发／生成文件，仅保留 288 个文件、5,366,436 字节。每份复制均与原文件 SHA256 相等，最终再次用真实 CLI dry-run 验证：

- 构建脚本 `scripts/offline-manifest.mjs` 包含在内；八份固定场景图片与类别生成路由完整。
- 凭据、环境文件、测试、合成数据、审校报告和旧生成缓存均不在上传文件中。
- 实际云端日志确认下载 288 个文件；实际上传增量约 2.3 MB，不能与源文件总大小混淆。

当前 CLI 的 curl 命令将 `--global-config` 错误转交系统 curl；改变参数先后仍失败。核对安装包解析代码后，仅为同一 CLI 使用其 XDG 支持的任务目录配置副本，未打印凭据、未改动系统代理或浏览器保护。此兼容路径与原认证目录均被排除上传。

## 尚未完成的网络验收

- 修正 CLI 参数问题后，系统 curl 对普通候选的 443 连接约 21 秒后超时。
- 独立 Node 只读请求对正式域名和普通候选均返回 `UND_ERR_CONNECT_TIMEOUT`（10 秒）。不能据此判定是应用代码故障，也未证实具体 DNS／运营商原因。
- Vercel 连接器访问受保护候选只返回 SSO 302，而非应用健康 JSON，不能记为应用通过；未关闭访问保护或将认证链接写入此记录。
- 未成功获取普通候选的健康响应、在线离线清单及其资源哈希，也未验证应急候选实际 HTTP 页面与构建 ID。
- 未执行推广、回滚、真实用户学习操作或正式数据导入。实体 iPhone 麦克风、音色、系统选区、键盘、安装和真实断网测试仍需真实设备验证。

## 继续条件与顺序

2026-09-13 用户要求重试后的复查：两个候选部署再次确认 READY，正式域名再次确认仍指向旧 EUHH 部署。使用已授权 CLI 请求普通候选健康接口，10,009 毫秒连接超时；同一电脑 Node 对照请求中 Vercel 官网 HTTP 200（454 毫秒），原应用健康接口仍 `UND_ERR_CONNECT_TIMEOUT`（10,662 毫秒）。这次没有重新上传、创建部署或改变域名／网络配置；结果表明管理服务可达但本次应用域名访问检查未通过，不证明具体运营商／DNS原因，也不代表应用 HTTP 功能已验收。下一步先由用户用常用浏览器或另一可用网络确认应用地址可达，再继续同一批候选的验证。

1. 先恢复当前电脑对 Vercel 应用域名的可用访问；无需重新授权账号，除非凭据确已过期。不要用清空学习数据解决网络问题。
2. 只读验证普通候选健康接口、发布 SHA、`MT7REtpdsjw4wZzxLgBzF` 对应清单、全部类别／资源字节与哈希，以及主要页面／安全头；只用 GET/HEAD，不导入合成记录到远端。
3. 验证应急候选健康模式、实际构建 ID、根页及深链只读展示；原先本地全 12 表恢复验证只作为对应源码的本地证据，不扩称为远端数据验证。
4. 保留旧版、普通候选和只读候选，全部线上门槛满足后才将普通候选推广至原网址；随即核对精确用户网址的健康响应、SHA 与资源，不只看控制台 READY。
5. 补齐最终推广时间、回退指令、线上响应与手机限制；不会把旧 2.3 直接回滚视为已升级设备的数据安全方案。

接口配置参考：[Vercel 部署配置](https://vercel.com/docs/project-configuration/vercel-json)。本轮仅使用其中独立部署的输出目录覆盖能力。
