# SpeakMate 3.0 独立网址发布记录

日期：2026-09-13（北京时间）。用户要求：**新版部署上线，不覆盖旧版网址，随后在 GitHub 补充说明。** 本记录替代此前“将 3.0 推广到原网址”的后续计划；历史候选和失败记录仍保留。

## 1. 新旧入口与部署映射

| 项目 | 新版 | 旧版 |
| --- | --- | --- |
| 公开入口 | [speakmate-v3.vercel.app](https://speakmate-v3.vercel.app/) | [speakmate-pwa.vercel.app](https://speakmate-pwa.vercel.app/) |
| 版本 | 3.0.0，严格本地学习 | 2.3.0，原入口保留 |
| 部署 ID | `dpl_8R4ZRiUuh8BGk2GZBUR5dAREW4rx` | `dpl_EUHHJUXwxLJw7FjFjntkrf2ZE1Fh` |
| 健康接口 SHA | `9b61835b873a` | `eb2a6e7f76a6` |
| 匿名首页 / 健康接口 | HTTP 200 / 200 | HTTP 200 / 200 |
| 重定向 | 无 | 无，不跳转新版 |

两者共用现有 `speakmate-pwa` 免费项目，但分别绑定不同部署。项目 ID `prj_EVHJZ8TczAF9yHrDjNz2aiBq0ItM`；未新建项目、数据库、模型服务、订阅、套餐或购买域名。`targets.production` 仍是旧部署，**不能只看项目默认生产状态判断新版是否上线**，必须看精确新域名映射与其匿名响应。

本次复用已经 READY 的普通版构建，没有重新上传或改变应用代码。候选来源完整提交为 `9b61835b873a64782242ef0d10eff1ed6bcc62c0`，实际应用逻辑与已审查的 `c2af5485bf8b73a22242594ae17504deac01c717` 一致；归档提交 `38c01e2f1609a3758ec6e82d5c7b162134439dfd` 与候选在 `src/`、`public/`、`scripts/`、依赖锁和应用配置无差异。此后 GitHub 提交补充的是文档与只读发布证据，不要求线上健康 SHA 变成文档提交 SHA。

## 2. 本次实际操作与访问问题处理

1. 先确认旧域名精确指向 EUHH 部署，候选 8R4 已 READY；使用原已授权账号及现有团队，没有读取或公开凭据。
2. 通过 Vercel CLI 59.16.0 将新 alias `speakmate-v3.vercel.app` 绑定候选。旧映射保持不变。
3. 匿名访问旧域名为 200，新 alias 为 SSO 302。只读检查发现项目保护为 `all_except_custom_domains`，而项目域名列表仅有旧域名；新 alias 尚未登记。
4. 执行 `vercel domains add speakmate-v3.vercel.app speakmate-pwa`，仅登记新版域名；随后再次将它 alias 到正确 3.0 候选。两个域名均 verified，无 redirect、无分支重定向。
5. 再次核对旧部署未变，项目访问保护未变，Git 集成仍未连接。匿名访问新版首页/健康接口已为 200，不需要读者登录部署账号。
6. 下载新版公开构建清单，逐项验证 93 项实际响应，另检 manifest、Service Worker 与能力接口。操作均为读取，没有创建会话、导入备份或提交练习正文。

没有执行全项目 Promote、Rollback、普通自动分配域名的生产发布，也没有关闭部署保护。生成的长候选地址仍可能需要部署账号登录；给用户分享上表新版公开入口，不分享带认证查询参数的链接。依据：[Vercel 域名登记](https://vercel.com/docs/domains/working-with-domains/add-a-domain)、[手动 alias](https://vercel.com/docs/cli/alias)、[部署保护](https://vercel.com/docs/deployment-protection)。

## 3. 在线核验与原始证据

资源核验时间：2026-09-13 11:54:45–11:55:17，北京时间。使用 PowerShell 7 `Invoke-WebRequest` 匿名 HTTPS GET；不附加账户凭据、共享访问 Token 或用户 cookies。旧域名在资源检查前后都返回相同的 2.3.0 与 SHA。

| 检查 | 实际结果 |
| --- | --- |
| 新版健康 | `status: ok`，`version: 3.0.0`，`mode: local-learning`，`aiMode: local`，`buildSha: 9b61835b873a` |
| 线上离线清单 | HTTP 200，构建 ID `MT7REtpdsjw4wZzxLgBzF` |
| 页面壳 | 16 / 16 HTTP 200，大小及 SHA256 相符 |
| JS / CSS / 字体 / 图标 / 场景图片 | 70 / 70 HTTP 200，大小及 SHA256 相符 |
| 分类语料包 | 7 / 7 HTTP 200，大小及 SHA256 相符 |
| 额外入口 | `/manifest.webmanifest`、`/sw.js`、`/api/v1/capabilities` 均 HTTP 200，类型符合用途 |
| 旧域名保护 | 版本、SHA、部署 ID 均保持原值，无重定向 |

原始逐项结果：[http-verification.json](../../outputs/qa/independent-url-2026-09-13/http-verification.json)。可复现的只读工具：[verify-independent-urls.ps1](tools/verify-independent-urls.ps1)。本次检查实际下载响应后计算 SHA256，没有用旧本地构建的哈希冒充线上结果。

此前 Node 直接连接应用域名仍有超时，本次 PowerShell 请求成功；没有修改系统代理、网络或浏览器保护设置，不据此推断具体运营商故障原因。历史原始失败见[候选部署记录](3.0.0-deployment-2026-09-13.md)。

## 4. 手机入口与数据迁移

1. iPhone 使用 Safari 打开新版公开地址，再选择“添加到主屏幕”，建议名称“SpeakMate 3.0”。旧主屏幕图标继续打开旧网址，不会自动升级为新版。
2. 新旧域名的 IndexedDB、缓存和本地身份隔离。发布不会复制、清空或替换旧学习数据；首次打开新版没有旧记录不等于旧数据丢失。
3. 有旧记录时，先从能看到记录的旧入口导出，确认文件保存，再到新版“我的”预览并确认恢复。尽量在新版开始积累学习数据前迁移；不同身份冲突先分别备份，不强行合并。
4. 部分合法旧记录仅作为历史保留，不能保证继续旧对话；旧练习不补发 3.0 任务积分。保留旧入口和原文件，不删除站点数据验证迁移。

详细步骤：[iPhone 安装](../guides/3.0-iphone-setup-and-checks.md)、[备份恢复](../guides/3.0-local-data-and-backup.md)、[使用教程](../guides/USER_GUIDE.md)。备份包含个人内容，不应上传 GitHub。

## 5. 后续维护约束与未验证范围

- 后续构建必须使用 `--prod --skip-domain`，核对后只把新版域名 alias 到新构建；禁止自动分配全部域名、整体 Promote/项目级 Rollback。当前两域名共用项目，不是自动发布隔离保证。步骤见[部署手册](../engineering/DEPLOYMENT_3.md)。
- 本次未连接 Git 集成，GitHub 文档 push 不主动触发应用部署。已有 ZIP / Git bundle 是其对应提交的不可变资产快照，不会随本次文档更新自动改写；最新说明以 GitHub main 为准。
- 本次没有运行用户学习操作或重跑整套应用回归，93 项通过是在线资源完整性证据，不是所有交互或 PWA 安装通过。此前限定源码/浏览器结果及原始失败见[验证记录](3.0.0-verification.md)。
- 实体 iPhone 麦克风、本地音色、系统选区、键盘、主屏幕与 Safari 数据关系、真实断网以及中国大陆所有运营商仍需分别验证。
- 只读应急候选的远端 HTTP 流程未在本次扩验；其独立域名无法读取新版域名的记录。真正数据恢复须满足同源、数据库 schema 和工作线程控制条件，不能把旧 2.3 当作新版安全恢复入口。
