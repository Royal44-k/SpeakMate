# SpeakMate 3.0 开发规格

版本：3.0.0-draft · 更新：2026-09-10 · 状态：已批准，实施中，未发布。

本文件是开发依据，不能把计划项当作已完成功能。生产仍为 2.3.0，来源 `eb2a6e7f76a6d24446e59bec6a0abc86731801d8`，部署 `dpl_EUHHJUXwxLJw7FjFjntkrf2ZE1Fh`，保留为旧部署基线；升级设备的安全回退还须满足第 8 节数据库兼容门槛。旧规格见 [2.3.0 归档](docs/releases/2.3.0-APP_SPEC.md)，实施见 [执行计划](docs/superpowers/plans/2026-09-09-speakmate-3.md)。

## 1. 产品与能力边界

面向中国大陆成人 A1–C1 学习者的移动 PWA：目标 → 练习 → 记录 → 理解 → 定向复习 → 奖励。

- 严格零费用；保留现有 Vercel 免费项目及 PWA 安装，不开通订阅、云数据库、云模型、付费监控、自动扣费或 MySQL。手机不依赖电脑开机。
- 练习正文、录音、笔记、任务和积分只在设备本地处理。公开静态语料可随页面下载并缓存；不向 API 或第三方发送学习数据。即使残留云环境变量，也不得暗中开启远程模式。
- 本地学习助手使用校审内容和规则；明确显示完整覆盖、部分覆盖、未收录，不冒充通用生成式 AI，不编造语法/语义/音素分数。
- 好友榜只展示演示数据，始终标注“示例数据，非真实好友排名”；不混入真实统计、备份账本、兑换及个人积分。
- 积分兑换仅限本地数字奖励，不涉及实物、现金、转赠、地址、物流。
- 保留未来认证、同步、内容、任务、学习助手、账本和榜单 Provider 接口；未来服务器可使用 MySQL，但本版不连接。

## 2. 页面、导航与设计系统

一级导航固定为：**目标 `/` → 练习 `/practice` → 场景 `/scenes` → 记录簿 `/notebook` → 我的 `/me`**。五个等宽入口，不横向滚动。欢迎、指南和设置引导归属目标。次级页有明确返回路径，保留原筛选、滚动、笔记和任务来源；会话的新建 ID 替换保留路由参数和恢复记录。

离线详情采用可缓存的固定客户端入口，查询只携带不透明标识和公开选项，正文从本机读取；跨页面外壳使用统一的文档导航并保留退出保护，旧链接精确兼容。完整路径、缓存类型隔离及取舍见 [离线路由契约](docs/design/3.0-offline-routing.md)。此规则尚待实施和浏览器验收。

延续 Dialogue Stage 成人编辑式视觉：Atlantic `#0B2A43`，Coral `#FF6B5E`，Sky `#DDEFFD`，Paper `#FFFDF8`，Barlow Condensed 用于英文展示，系统中文字体用于正文。内容最大宽度 480px；44px 以上触摸目标；安全区、键盘、底栏留白；320–430px/横屏/200% 字体无覆盖、截字、横向溢出。徽标在正常文档流，不压住正文。主要签名是纸页式学习记录与明确的每日三步进度，不增加儿童游戏化装饰。

`/` 首次显示欢迎，完成引导后显示今日计划。标题分为“随时开口，”和“练真实英语”完整短语，“随时开口”不拆行。按钮“开始练习”。介绍末尾增加“随时随地自在说英语！”。引导保留等级、兴趣、每日 5/10/15 分钟；已有用户编辑时按钮“保存设置”。可重新打开使用指南。

练习页提供新一轮在上、继续旧对话在下的选择，不替用户销毁进行中的记录；等级来自用户与当前会话真实数据。场景保留现有搜索、清空、搜索按钮和筛选。

## 3. 今日目标、打卡与奖励

每天依据北京时间、用户等级/兴趣/时长、最近场景、到期笔记生成并保存稳定计划：

1. 表达热身：回忆收藏表达；无记录时使用本等级入门表达。
2. 场景应用：匹配等级、兴趣、时间的场景，避免连续重复。
3. 巩固练习：用今日表达完成记录簿微练习。
4. 15 分钟计划有一项可选拓展。

每卡列出目的、预计时长、具体完成条件和直接入口。每个未开始槽位每天可换一次；开始/完成后不可换。修改今日时长保留已完成/已开始项与奖励；降到 5/10 分钟不能删除已开始的可选项。同一天刷新、重开或多标签页不能重抽或重发积分。非今天任务不能在今天另发一次奖；跨日会话仍可完成原来源任务，以真实完成日期打卡，奖励键绑定原任务槽位。

完成任一有效学习任务自动打卡，打开页面不算。任务进度与前台练习时长分开统计；后台/页面隐藏不累计时间，不将时长当作口语能力评分。提供月历、连续天数、近七日趋势与周回顾。中断不扣分。提醒限应用内。

每个核心任务 10 分，三个核心全完成加 5 分，可选拓展 10 分；每份计划合计最多 35/45 分。跨日补完成归属原计划，不另设会吞掉原任务奖励的自然日入账上限。奖励不要求零错误。旧历史不追溯积分。累计获得与可用余额分别从账本计算，不信任外部余额字段。每个任务/全勤事件/兑换都有稳定唯一键；事务保存完成事件、计划状态和账本，失败全部回滚。并发兑换不能负余额；已拥有奖励不重复扣分。

六个首发奖励：2 款个人卡片样式（100 分）、2 款目标卡封面（200 分）、2 份原创便携学习资料（300 分）。兑换后立即查看/应用，展示已拥有/当前使用/兑换历史，核心练习不锁。示例榜与真实个人成绩分开。

## 4. 记录簿与本地学习助手

三个板块：词句、待复习、模拟练习。

- 在对话历史（双方）、参考表达、单轮反馈、场景报告中可原生长按/滑选；另有“记录”整句按钮及选词替代路径。原生复制、选区手柄、滚动保留；不可全局阻止默认触摸行为。
- 保存前预览原文、场景及上下文；保存后“查看/撤销”，不强制跳走。类型分单词/短语/整句。保存来源 sceneId/turnId/level、上下文副本、备注、标签、时间。
- 标准化大小写、空白、首尾标点去重，保留原始展示文本，合并来源。支持搜索、筛选、编辑、删除和撤销。删除历史不删除笔记快照。旧 favorites 迁移保留稳定 id/来源且只迁移一次。
- 已校审解析包含语境中文义、词性/句法/搭配、正式程度和使用场合、常见错点/易混表达、分级例句/替换及练习入口。原文、校审解析、个人备注分别展示。未覆盖不拼装确定结论，只开放保存、自我回忆、备注与跟读。
- 复习间隔默认 1/3/7/14/30 天（产品策略，不宣称个人最优）。自评“记得/模糊/忘记”调整下次日期，自评与规则反馈明确区分。
- 模拟练习：遮盖回忆 → 替换或造句 → 3–5 轮情境应用；携带来源笔记、taskId/planDate、合法 returnTo，结束回原笔记或复习队列。不把填入参考答案伪装成已完成练习。
- 空状态、加载、无覆盖、保存失败、存储不足与撤销均有明确反馈。个人历史提供完整列表和删除入口，不仅前六项。

## 5. 语料与对话引擎

42 场景、7 类保留，每个场景每个 A1/A2/B1/B2/C1 等级至少 12 个问法、6 类交际意图，每问 2 个相关回应，总目标 ≥2,520 问法及 ≥5,040 回应。不能以同一题简单填充长度假装分级，也不能把重复句字符串当成多样性。

|等级|交际能力|
|---|---|
|A1|简单需求、身份信息、固定表达、基本问答|
|A2|日常交换、偏好、简单原因、澄清|
|B1|经历、解释、建议、常见问题处理|
|B2|协商、比较、论证、不同意见|
|C1|语气分寸、隐含条件、复杂取舍、表达重述|

短练/标准/拓展以约 3/6/10 个用户轮次组织，估计时间不是等待计时。对话显式定义事实、意图、前提和结束条件；按已完成意图/事实推进，不重复询问已确认信息。支持澄清、拒绝、改主意、偏题和表达困难。变体只采用校审组合，结束后停止提问。

分七类逐包执行“资料查证 → 原创编写 → 分级审校 → 连贯性审校 → 自动校验 → 发布”。每条有版本、等级、意图、来源依据、审校状态。记录真实审校方法，不将程序自动通过等同于专业人工审校；有争议内容保持草稿。不批量复制课程、付费题或受保护音频。

依据：[CEFR 描述](https://www.coe.int/en/web/common-european-framework-reference-languages/cefr-descriptors)、[British Council A1–B2 口语](https://learnenglish.britishcouncil.org/free-resources/speaking)。C1 以 CEFR 为依据，不声称 British Council 此页覆盖 C1。

## 6. 严格本地语音

- 正文提交直接调用本地引擎，不先请求 `/api/v1/turns`。云端适配器不在默认运行路径。
- 自动转写必须确认 `processLocally` 能力和本地语言包可用；不确认时绝不启动标准/前缀服务器识别。采用录音回听＋文字确认；空文字不能发送或假称识别成功。
- 录音临时存在当前会话，离开释放，不写长期备份，不上传。30 秒上限，保留拒权、无声、太短、来电/切后台恢复。
- 朗读只用 `localService === true` 的英语音色，没有时明确告知，不退到未知默认音色。存储的语速、自动播放、反馈展开贯穿所有入口；播放错误可见但不阻塞文字学习。
- [MDN 语音说明](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API/Using_the_Web_Speech_API)：默认 Web Speech 可能发送音频到服务器，不能称为离线能力。

## 7. 架构、迁移、恢复

沿用 Next.js 16、React、TypeScript、Zustand、idb；不重建、不增依赖服务。域规则为纯函数，持久层实现可替换 ports，界面不直接操作 objectStore。

IndexedDB 数据库名仍 `speakmate-v1`，升级版本（不删除重建）。增加 notebook、reviews、dailyPlans、learningEvents、pointsLedger、rewardUnlocks；保留 profile/sessions/turns/favorites/settings。旧收藏迁移稳定，避免每次打开覆盖用户编辑。记录多标签升级阻挡与配额不足，允许重试/导出，不静默丢历史。

导出 schemaVersion 2，包含全部本地用户数据、状态、版本和时间，不含录音/令牌/演示榜。导入同时支持旧 schema 1；Zod 严格结构、大小/数量/日期/枚举/关系检查，预览数量与冲突，用户明确确认后事务合并。不执行 HTML，重复恢复不重复事件/奖励/记录；余额由合法账本推导，缺少关联的奖励记录拒绝而不是补发。

旧版对话保留为可读历史，新版明确另开，不原地转换或套用旧完成证据。新版采用受保护的会话／话轮原子保存；正常对话终局保留待确认状态，用户确认后才与完成事件和适用奖励一起提交。语料未识别不等于学习失败，纯帮助点击也不冒充开口。兼容、流程判定版本及跨模块归属见 [练习保存与完成契约](docs/design/3.0-practice-persistence.md)，其中约定尚待实施验证。

本地学习数据可能被清缓存/设备回收清除；提供定期备份提醒和持久存储能力说明，无跨设备自动同步。本地积分可被用户修改，不能证明真实排行榜成绩，未来后台不直接接收为已验证余额。

## 8. 质量门与发布

每任务 RED（真实行为失败）→ GREEN（实现通过）→ 独立规格/代码审查。单测覆盖本地域规则、fake-indexeddb 迁移和原子性；浏览器 E2E 覆盖完整路径、断网、网络无数据上传、备份恢复、重复事件、多标签、跨日和权益兑换。

按[尼尔森十大原则](https://www.nngroup.com/articles/ten-usability-heuristics/)核查状态可见、用户控制、习惯一致、防错、识别而非记忆、效率、克制、恢复与帮助。按 [Apple 标签栏](https://developer.apple.com/design/human-interface-guidelines/tab-bars)核查一级导航。

覆盖 320–430px、横屏、200% 文字、键盘、安全区、长正文、返回来源、筛选/滚动恢复，手势与按钮两条路径。42×5×3 内容检查包括意图推进、事实一致、终止不再发问。虚拟榜始终标注示例并与真实奖励隔离。

完成 lint、类型、单测、生产构建、浏览器回归及截图审查后才更新 3.0 生产版本；保留回退部署，更新 PWA 缓存，不清用户库。仅使用已有免费 Vercel。发布成功不代表大陆各运营商或实体 iPhone 已验收，应单列真实完成与未验证项目。

回退构建必须兼容已升级的 IndexedDB v2。原 2.3 客户端指定打开 v1，不能直接作为升级设备的数据安全回退；发布前应准备并验证兼容 v2 的回退构建，保留全部新存储，不降级或删除用户库。

## 9. 决策记录

- 2026-09-10：旧记录只读保留、新练习明确另开；备份有效与可继续分别校验，不增加无必要的记录版本标记。代价是旧版未完成轮次不能原地续接，需要清楚的恢复说明。
- 2026-09-10：对话终局与用户确认完成分离；发布前将完成状态、事件与奖励接入同一事务。正常流程允许未覆盖表达及混合帮助，纯帮助或提前放弃不算完成；总提交与实际表达分开统计。代价是采用宽松的投入判定，并增加原子性与确认恢复测试。

- 2026-09-09：用户批准完整 3.0 计划，随后允许独立工作区；`feature/speakmate-3` 基于 `aacbffd`。
- 免费/本地约束取代 2.3 的云优先回退策略；保留后台接口不是启用后台。
- 先记录生产回退点；开发期间正式站点不变。
- 2026-09-09：为支持断网后新建记录及刷新，采纳固定客户端页面外壳与统一文档导航；代价是跨页重载，需要明确保存来源与滚动状态，不引入新框架或后台。
