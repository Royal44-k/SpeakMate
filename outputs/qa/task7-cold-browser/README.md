# 首次语料准备与离线组件异常：真实浏览器验证

2026-09-12，原 Task7 实施者明确交接的有界 QA。控制器只编写测试和证据，不修改应用，不接触正式数据，不新增费用。冻结的普通构建：`in2pG1ng-2S7-GB53eqAC`。

## 方法与边界

复用自有服务器生命周期 `tests/e2e/task7-owned-local-server.ts`，启动／退出只属于本测试的 Next 3132。测试再启动独占的 HTTP 3135 代理，原样转发实际 HTML、RSC、公共包和资源；只有明确命名的 `/sw.js` 故障改变该脚本响应。每项使用新建的隔离浏览器 context，业务状态全部由真实设置与任务 UI 创建，没有伪造积分、任务或 worker 消息。

原生注册失败通过脚本 HTTP 503 触发，不替换 `register()`。未控制窗口通过暂缓脚本响应产生，不模拟 `controllerchange`。旧 worker 使用 `git show aacbffd:public/sw.js` 的原始字节，SHA256 为 `781e3b246b7edcec11098b66f7883ba168b5436df21ebcfdd84340f0b35d50d3`；先在无应用脚本的同源 JSON 文档中真实注册 `/sw.js?v=2.3.0`，再访问新版应用。此用例后续脚本请求也提供同一旧字节，因此只证明“真实旧 worker 缺少新版证明”的失败边界，不冒称完整 2.3 部署到 3.0 的升级验证。

“不支持”一项明确在文档启动时移除 `navigator.serviceWorker` 特性，是能力故障模拟，不是实体手机或操作系统检测结果。全部使用真实时间，不提前触发十秒计时器。

## 五项结果

|实际场景|Chromium 手机模拟|Windows WebKit iPhone 模拟|实际结算与数据结果|
|---|---|---|---|
|最初尚无控制器，准备期间放行真实注册|通过，3.5秒|通过，9.0秒|等待中的准备继续；仅查看参考时全部12库不变，真实完成热身后1笔10分|
|超过十秒控制器等待期限|通过，13.1秒|通过，20.5秒|错误前后12库完全相同、零积分；注册恢复后点击原准备按钮可完成，仅1笔10分|
|原生脚本503导致注册失败|通过，13.1秒|通过，22.6秒|失败不改变数据；解除故障、刷新、手动准备后完成10分，无清缓存／清数据操作|
|明确模拟浏览器不支持该能力|通过，1.6秒|通过，8.8秒|很快提示，任务可重试，12库不变、零积分，未发起脚本注册请求|
|真实旧2.3 worker 没有新版语料证明|通过，12.1秒|通过，19.5秒|原控制器 URL 为2.3、原缓存存在；十秒后提示，任务未开始且12库不变|

上表耗时是测试项耗时。实际准备失败等待另存 JSON：Chromium 的控制器／503／旧版分别为10183／10150／10155ms，WebKit 为10795／10519／10252ms；不支持项分别42／211ms。没有把全部后续操作时长解释为十秒协议期限。

8张错误页面全页截图均重新打开：提示写明“所选任务保持原样”，同时提供“重新读取”和离线更新说明；热身仍“尚未开始”、积分为0。全页图中的固定底栏记录在截图时视口底部，不能仅凭它出现在整幅长图中部推断固定栏布局错误。此专项没有进行独立的44px／200%文字测量，相关证据另见手机专项。

实际重新读取每份状态和网络附件：恢复成功的6项各只有一笔10分；8项错误状态均全12库逐值相同。Playwright页面请求记录为Chromium511条、WebKit204条，均同源GET/HEAD且无页面异常；HTTP代理另外记录475／485条全为GET/HEAD，包含原生 worker 请求。两种观测层有缓存及worker可见性差异，不能将两组相加称作独立请求总量。

## 命令与记录

`N` = 本工作树 `.superpowers/sdd/2026-09-09-speakmate-3/runtimes/node-v22.23.2-win-x64/node.exe`。仅本命令环境设置：

```powershell
$env:PLAYWRIGHT_BASE_URL = 'http://127.0.0.1:3135'
$env:TASK7_EXPECT_BUILD_ID = 'in2pG1ng-2S7-GB53eqAC'
```

- 仅新测试 Prettier 与 ESLint：exit0，无 lint 诊断。
- `N node_modules/@playwright/test/cli.js test tests/e2e/task7-cold-browser.spec.ts --project=chromium-mobile --output=outputs/qa/task7-cold-browser/run-01 --reporter=list`：**5项通过，46.7秒，exit0**。
- 同命令换 `--project=webkit-iphone --output=outputs/qa/task7-cold-browser/webkit-01`：**5项通过，1.4分钟，exit0**；不是Chromium耗时。
- `N node_modules/typescript/bin/tsc --noEmit`：exit0，无输出；适用于执行时源码，不替代最终全量检查。
- 两次保留原 `NO_COLOR`／`FORCE_COLOR` 环境提示。测试生命周期自动结束自己的服务器与代理；最后只读检查3132／3135无监听。没有停止原实施者3130／3133或未知服务。

先前奖励专项中首次准备失败的原图和trace仍保留。这次证明设计决策20的等待／拒绝／重试路径正常，不删除原失败或将最初缺失的就绪前提追认成当时已通过。内容损坏、证明字段失配、多窗口更新、完整离线闭环与最终构建由原任务其余测试负责。本项通过不表示整体发布门通过，也不表示实体 iPhone、飞行模式或中国大陆网络体验已验收。
