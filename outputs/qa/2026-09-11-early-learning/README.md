# 今日目标 → 热身：早期界面检查

时间：2026-09-11。来源运行时代码 `4d16e30`，生产构建 `3Ub4KxuDKoDCl9Zk_0UNG`。此为 Task7 修复前的真实内置浏览器局部检查，不是最终自动化验收或发布证明。

独立测试源为 `http://127.0.0.1:3128`，只通过可见界面新建合成 A2／旅行／每天 5 分钟资料。未使用实际用户记录、没有录音、没有导入或清除数据。浏览器窗口实测 395×556，文档内容宽 383px；没有把它当作精确的 iPhone 模拟。三张原始截图均保存后重新打开检查；没有图像编辑。

## 1. 欢迎入口：当前观察正常

![当前欢迎页](01-welcome.png)

“随时开口，”保持同一行，按钮为“开始练习”，末尾新增文案和五项导航均可见。文档没有横向溢出，没有框架错误覆盖层；读取的错误／警告日志为空。标题有当前焦点轮廓，不把它当作图片标注。

## 2. 保存引导后进入目标：入口正常，短窗口首屏任务不可见

![今日目标首屏](02-goal-entry.png)

实际点击完成 A2、旅行和默认 5 分钟引导后生成当日计划；中间三个选择页面只作为准备操作，没有逐页截图，不宣称这一组图完成了整个引导审查。

目标页显示三项尚未开始的核心任务、累计与余额均为 0，没有因打开页面打卡。首屏主要被说明与设置卡占据，任务需要向下滚动；这是后续精确尺寸矩阵的可发现性检查项，不是已经证明的内容遮挡。

## 3. 点击“准备表达热身”：发现进入反馈断点

![点击后仍停留在原任务卡](03-warmup-reference.png)

该文件记录的是点击后的实际可视区，**不是热身参考卡已可见的证明**。真实按钮点击后，公开资料加载完成，页面出现 `black` 的情境释义和“隐藏参考，开始回忆”按钮，但新流程被插在当前视口上方；画面中仍是原任务及灰掉的按钮，焦点落在 `body`。

只读 DOM 测量：`scrollY=1269.2308349609375`；“表达热身流程”区域相对视口 `top=-583.3253784179688`、`bottom=-95.8734130859375`。即整个新流程在可视区上方。此时实际地址仍为 `/`，累计／余额仍为 0，错误／警告日志为空。

这是状态可见性、用户控制和下一步可发现性的实际问题，已交 Task7 原实现者修复，并要求通过真实消费者测试与最终浏览器点击后的焦点／视口检查复核。不能用测试定位器自动滚动到下一按钮来掩盖这个断点。此次检查在最早失败边界停止，没有继续写回忆、完成任务或领取积分。

## 限制与清理

未验证后续回忆／计分／模拟／离线、网络隐私、200% 文字、键盘、VoiceOver、实体 iPhone 或中国大陆运营商网络。本轮浏览器页签已关闭，临时服务已停止；该独立源的合成资料可保留，不清除任何日常浏览器数据。
