# Whole-branch final fix evidence

FIX_BASE `d73b698c3ea8a352c2561ee7c2148134453a14b1`. Author verification only: I1/I2/I3 and bounded M1 implemented; M2 deferred. No final scoped approval, deployment or physical-device claim. Full report: `.superpowers/sdd/2026-09-09-speakmate-3/final-fix-report.md` in the worktree.

## Current evidence

- [Covering](covering.json):14 files134 PASS/1 fixture-mock FAIL; [corrected full rounds file](covering-rounds-corrected.json):9 PASS. [Adjacent fixed next links](green-fixed-next-link.json):both complete rounds/stage files23 PASS, including3 new true-link consumers. Thus138 distinct relevant tests covered across these runs, not one all-green full-suite command. Original6408 PASS/1 FAIL and subsequent25 PASS historical record unchanged.
- [Final build](build-last.json) and [manifest](manifest-last.json): normal **IlMT-x-bkZpDjbraeJv5i**,16 shells/70 assets,5,020,405 shared bytes,5,411,091 category bytes. Intermediate gjS0 build is retained in build.json because a subsequent source self-review found the fixed goal/simulation next-link regression. Its server was stopped before the final rebuild.
- [Final two-engine path](browser-source-ready.json):2 PASS/34.0s (Chromium5.8s,Windows WebKit25.3s). Actual report bookmark, contextual preview/save, canonical duplicate undo, note/source link, notebook list, restored session→planned successor, immutable original. `source-ready/*/state-and-layout.json` retains original/next/note and exact44px layout results. `source-ready/*/network.json` explicitly records request/error arrays.
- [Final TypeScript](typecheck-source-ready.json), [scoped lint](lint.json), [late source/link lint](lint-last.json), [final test/audit lint](lint-source-ready.json):exit0. Final type/lint include the final source-link assertion and formatting. Build type checking includes all final application code; subsequent browser-only assertions do not alter production.
- [Evidence index](evidence-index.json):selected originals' byte sizes/SHA256, final manifest hash, unchanged recovery build ID and final network audits. `audit.mjs` is read-only except writing this index; raw command capture uses `run-evidence.mjs`.

## RED and harness failures are not interchangeable

`red-notebook.json`:persisted0.8/1.15 became1; settings-read failure omitted error;40 notes caused40 counted reads vs2. `red-consumers-threads.json`:free successor incorrectly counter and invalid predecessors created records. Its initial I3 short unsupported phrase fixture was wrong. Corrected `red-bookmark.json` proves actual bookmark lacks preview while detailed capture passes. `red-fixed-next-link.json` proves3 actual fixed-task/simulation next links rejected by the initial new predecessor validation. Fixed links now create ordinary unbound practice; saved record/plan equality asserted. `green-consumers-01.json` retains first4-file28 PASS.

M1 uses40 notes(one deleted),12 representative histories, aliases/tied heads,37 due notes and hand-derived expected IDs. One aggregate existing storage read replaces per-note reads; no timing/OOM claim. TTS uses real persisted settings/repositories and only local platform voice stubs, with stop/unmount/read-error/no-remote-voice checks. Bookmark unit tests retain equal text from two actual questions, atomic canonical undo and source-history deletion plus memory/IndexedDB backup destinations.

First test startup-only interruption has no assertion result (`red-initial-interrupted.md`); no cause inferred. Old server absent/refused is setup, not app RED. Baseline retry/control had wrong finish label/assumed automatic navigation; baseline-report correctly reached original k0 bookmark and failed missing preview. Final/corrected browser attempts had wrong canonical-ID and physical-delete expectations; production semantics were not changed. `browser-last.json` retains Chromium PASS/WebKit third-reference timeout; waiting actual committed counters fixes the test's premature next action. `browser-settled.json` passes both32.0s; source-ready adds exact async source-link readiness before the final note screenshot.

## Visuals and limits

Two original `baseline-report-chromium` bookmark PNGs were opened at320/100 and390/200. All12 `settled` PNGs were individually opened: both engines' bookmarks, preview at both sizes,200% scrolled save and320 note. No new bookmark clipping/overlap; existing preview scroll exposes save/cancel. Long source IDs wrap. Settled WebKit note image preceded async source-link readiness, so use final `source-ready` note images (also opened) for resolved source links. Other settled images remain current-build visual evidence.200% is root text sizing, not OS keyboard, selection or zoom certification.

Only isolated synthetic local contexts, same127.0.0.1 GET/HEAD. NO_COLOR/FORCE_COLOR, Git LF/CRLF and raw error-context trailing spaces retained separately; no suppression/normalization. ZIP traces stay local, not committed; no historical untracked files staged. Own final3130 server session54734 remains available. Recovery `fSQ0KjjPzm1yRggFV4QwF` unchanged because normal learning owners are not mounted there; final-source recovery/upload/compatible rollback/deployment and mainland/physical iPhone/Android gates remain Task8. This is not independent approval.
