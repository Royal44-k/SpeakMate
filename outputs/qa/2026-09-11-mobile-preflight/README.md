# 手机界面初步观察：不是最终验收

2026-09-11；源码 `b41089bd93dcfa8aeb55b5d472f7ec7eaaa1d7ca`，生产构建 `8LPYZAUq7DYOaivOPkppV`。后续独立审查要求修复返回语义与历史读取，以下只保留修复前观察，不作为修复后证据。

1. 欢迎首页：初步正常。完整短语、指定文案与五项导航可见；按钮盒约 68×63 CSS px，首页没有观察到水平内容溢出，浏览器错误／警告查询为空。标题有可见焦点边框，尚未验证键盘完整行为。截图为实际捕获的 `01-welcome-actual-395x556.png`，保存后已重新打开检查。
2. 点击“开始练习”进入 `/welcome`：初步正常。等级选择页实际出现，默认 A2；尚未选择等级、完成引导或检查后续保存。截图为 `02-onboarding-level.png`，保存后已重新打开检查。

截图与 DOM 的尺寸不能混同：工具设置为 390×844 时，实际 DOM 返回过 395×556 和 300×649，未成功建立精确视口。已恢复默认视口（DOM 395×556、内容宽 383、devicePixelRatio 约 1.95）。这些图片不证明 390px、320–480px 全矩阵、200% 文字、键盘、安全区、iPhone 或 VoiceOver 通过。

内置浏览器隐藏窗口首次连接超时；按其文档改为可见窗口后成功。没有使用独立 Playwright，没有浏览器断网或网络正文轨迹证据，没有完成学习闭环。没有麦克风授权、清除用户数据、使用正式站点学习记录或发布。为后续修复构建停止了本次创建的 3126 服务，并关闭临时预览页；原 3114 服务未改动。

![步骤1：欢迎首页](./01-welcome-actual-395x556.png)

![步骤2：等级引导入口](./02-onboarding-level.png)
