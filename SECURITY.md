# 安全与隐私交接

3.0 当前学习能力严格本地，不要求云服务秘密。`src/infrastructure/local-runtime-policy.ts` 固定关闭远端学习入口；不得通过旧配置重新开启数据上传。

## 不能公开的内容

密码、API Token、`.env.local`、CLI auth 配置、浏览器 profile/会话 Cookie、真实学习 JSON、私人录音、完整个人对话、带秘密查询参数的网址。日志与截图也需检查；`.gitignore` 无法移除已经进入 Git 历史的秘密。

公开测试只用合成数据与独立测试来源。备份校验/恢复测试不能通过清空用户实际库完成；新旧域名、Safari 与主屏 PWA 的数据不可假定自动共享。

## 报告问题

普通故障可提交去隐私的复现步骤、设备/浏览器版本、源码/构建 ID 和错误信息。安全问题涉及秘密时先通过仓库所有者已公开且你信任的私密联系渠道报告；本次未建立专用安全邮箱或保证响应时限，也不假称已启用 GitHub 私密漏洞报告。

发现疑似泄漏时不要在公开 Issue 贴出秘密。由凭据所有者撤销/轮换受影响凭据，评估历史清理；不要擅自 force-push 或删除审计证据。

## 发布与数据保护

导入先做大小/格式/关联校验，再预览与事务写入；积分以账本及事件一致性验证，不信任输入余额。应急版只读兼容 v2 数据，不降级或清库。未来服务器不能把本地积分当成防篡改资产。

本次归档扫描结果只代表明确规则的检查范围，不是安全认证。后续改动需重新审查；不将曾经通过的哈希清单当作永久安全保证。
